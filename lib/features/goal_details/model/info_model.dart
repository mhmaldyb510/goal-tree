class InfoModel {
  final String infoKey;
  final String infoValue;

  /// This create a Info Model that used in Info Table
  /// the [infoKey] is the key of info and the first bart in the table 
  /// the [infoValue] is the value of info and the second part of the table
  /// the [infoKey] and [infoValue] must be not empty



  InfoModel({
    required this.infoKey,
    required this.infoValue,
  });
}
